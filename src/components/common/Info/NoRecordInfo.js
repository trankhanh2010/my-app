import React from "react";

const NoRecordInfo = ({ }) => {
    return (
        <div className="no-record">
            <p>Chưa chọn bản ghi để xem chi tiết.</p>
        </div>
    );
};

export default NoRecordInfo;
