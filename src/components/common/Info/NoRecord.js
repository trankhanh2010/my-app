import React from "react";

const NoRecordInfo = ({ }) => {
    return (
        <div className="no-record">
            <p>Không có thông tin.</p>
        </div>
    );
};

export default NoRecordInfo;
