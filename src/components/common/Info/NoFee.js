import React from "react";

const NoRecordInfo = ({ }) => {
    return (
        <div className="no-fee">
            <p>Không có phí cần thanh toán.</p>
        </div>
    );
};

export default NoRecordInfo;
